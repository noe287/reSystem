DIFF_NAME=$1_`date +%h-%d-%H_%M_%S_%a`.p
#DIFF_NAME="$1.p"

git diff > $DIFF_NAME

cp $DIFF_NAME $patches/

cd $patches/
svn add $DIFF_NAME

svn ci -m $'Issue #1000001 \n\n--Uploading patch file.\n\n'

